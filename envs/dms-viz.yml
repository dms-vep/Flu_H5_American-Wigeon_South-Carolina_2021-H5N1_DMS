name: dms-viz

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - biopython=1.83
  - jupyterlab
  - pandas=2.1 # required because of https://github.com/matsengrp/multidms/issues/128
  - pip
  - pyarrow
  - python=3.11
  - pip:
      - configure-dms-viz==1.3.3
